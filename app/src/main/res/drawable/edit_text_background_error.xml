<!-- edit_text_background_error.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Cambia el color de fondo cuando hay un error -->
    <corners android:radius="8dp" /> <!-- Define esquinas redondeadas -->
    <stroke
        android:width="2dp"
        android:color="#F8D7DA" /> <!-- Cambia el color del borde a rojo -->
</shape>
